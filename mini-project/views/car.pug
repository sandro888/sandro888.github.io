doctype
html(lang="en")
    head
        meta(charset="UTF-8")
        
        title List
        style
            include style.css
            
    body
        a(href="http://localhost:3000/person") Back
        br
        br
        div(id="cars")
            h2=`VIN : ${mono.VIN}`
            h2=`Model : ${mono.Model}`
            h2=`firm: ${mono.firm}`
            h2=`number : ${mono.number}`
            h2=`color : ${mono.color}`
            h2=`owner : ${mono.owner}`
                button(type="submit" class="button" id="edit") Edit 
                form(action="http://localhost:3000/cars/" method="post" id="formEdit" hidden )
                    label(for="carFirm") 
                        input#carFirm(type="text" name="firm"  placeholder="firm")
                    label(for="carModels") 
                        input#carModels(type="text" name="Model"  placeholder="Model")
                    label(for="carVIN") 
                        input#carVIN(type="text" name="VIN"  placeholder="VIN")
                    label(for="carGNumber") 
                        input#carGNumber(type="text" name="number"  placeholder="number")
                    label(for="carColor") 
                        input#carColor(type="text" name="color"  placeholder="color")
                    label(for="carOwner") 
                        input#carOwner(type="text" name="owner"  placeholder="owner")
                    button(type="submit" class="submit") edit  
    script(src="/main.js")    
        